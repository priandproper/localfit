node_modules/
dist/

# Personal data — never commit. localStorage-first; the local backend holds the live copy.
data/state.json
data/*.json
public/state.json

# Local TLS certs / keys for the HTTPS backend — never commit.
certs/

.DS_Store
*.log
.env
.env.*
